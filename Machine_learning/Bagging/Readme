Bagging is a technique in machine learning which will combine the various model prediction to improve the accurac score and its overall performance.It also
reduce the variance of the predictions.it involves training multiple models on different subsets of the training data this uses bootstrapping technique where
samples are randomly selected with replacements 
