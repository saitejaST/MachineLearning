A decision tree algorithm is used in classification and regression problems its a tree like model where it will make decisions based on the sequence of rules
and conditions which are learnt from the training data .It will split the dataset into subsets based on the value of the input features this algorithm can be 
improved by the random forest methods,ensemble etc this type of algorithm is mainly used for the customer segmentation,credit scoring,medical diagnosis
